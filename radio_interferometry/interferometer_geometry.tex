\input{../tikz-header.tex}
\setmainfont{Libertinus Serif}
\setsansfont{Libertinus Sans}
\setmathfont{Libertinus Math}

\newcommand{\proj}[2]{\langle #1,#2\rangle}

\begin{document}
  \begin{tikzpicture}
  
    % basic positions
    \coordinate (T1) at (-2, 0);
    \coordinate (T2) at (2, 0);
  
    \coordinate (SRC) at (2.5, 3);
  
    % ground
    \draw (-3, 0) -- (3, 0);

    % telescopes 1 and 2
    \fill (T1) circle (0.5mm);
    \draw[anchor=north east] node at (T1){$p$};
  
    \fill (T2) circle (0.5mm);
    \draw[anchor=north west] node at (T2){$q$};
  
    % baseline
    \draw[->, >=stealth, line width=0.15mm, catblue] (T1) -- (T2) node[midway, below, catblue] {$\vec{b}$};
  
     % projected baseline
    \coordinate (BP) at ($(SRC)!(T2)!(T1)$);
    \draw[<-, >=stealth, line width=0.15mm, catpeach] (T2) -- (BP) node[catpeach, midway, sloped, below]{$\vec{b}_{\text{proj}}$};
  
    % source vector
    \coordinate (S2) at ($0.4*(SRC) - 0.4*(T1)$);
    \coordinate (S1) at ($0.3*(T2) - 0.3*(S2)$);
  
    \draw[densely dashed] (BP) -- (SRC);
    \draw[densely dashed] (T2) -- +(S2);
  
    \draw[->, >=stealth, line width=0.15mm, catred] (BP) -- +($0.5*(S2)$) node[catred, midway, sloped, above]{$\hat{s}$};
    \draw[->, >=stealth, line width=0.15mm, catred] (T2) -- +($0.5*(S2)$) node[catred, midway, sloped, above]{$\hat{s}$};
    
    % \alpha angle
    \draw[catgreen] ([xshift=1cm]T1) arc [start angle=0, end angle=34, radius=1cm] node[catgreen, left, midway, yshift=-0.08cm]{$\alpha$};
  
    % geometric delay
    \draw[catmauve] (T1) -- (BP) node[catmauve, midway, sloped, above, line width=0.15mm]{\footnotesize$\left| \vec{b} \right|\cdot\cos\alpha = \proj{\vec{b}}{\hat{s}} = \tau_g\cdot\symup{c}$};
  
  \end{tikzpicture}
\end{document}
